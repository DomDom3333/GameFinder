<UserControl
    x:Class="ArcadeMatch.Avalonia.Controls.Swiping"
    x:DataType="sessionVm:SwipingViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:sessionVm="clr-namespace:ArcadeMatch.Avalonia.ViewModels.Sessions"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowDefinitions="Auto,*">
        <Border Background="#333" Padding="20,10">
            <Grid>
                <TextBlock
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="🎮 Game Discovery" />
                <Button
                    Command="{Binding LeaveCommand}"
                    Content="🚪 Leave Session"
                    HorizontalAlignment="Right" />
            </Grid>
        </Border>
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel
                HorizontalAlignment="Center"
                Margin="30"
                MaxWidth="900"
                Spacing="20"
                Width="500">
                <TextBlock
                    FontSize="20"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Text="{Binding GameName}" />
                <Border CornerRadius="12">
                    <Image
                        Height="200"
                        Source="{Binding GameImage}"
                        Stretch="UniformToFill"
                        Width="400" />
                </Border>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                <StackPanel>
                    <TextBlock Text="🏷️ Genres" />
                    <TextBlock Text="{Binding Genres}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="🌍 Languages" />
                    <TextBlock Text="{Binding Languages}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="👨‍💻 Developer" />
                    <TextBlock Text="{Binding Developers}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="📅 Release" />
                    <TextBlock Text="{Binding ReleaseDate}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="💰 Pricing" />
                    <TextBlock Text="{Binding Price}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="⭐ Metacritic" />
                    <TextBlock Text="{Binding Metacritic}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="🗣️ Reviews" />
                    <TextBlock Text="{Binding Reviews}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"
                    Orientation="Horizontal"
                    Spacing="20">
                    <Button
                        Command="{Binding DislikeCommand}"
                        Content="👎 Pass"
                        Height="50"
                        IsEnabled="{Binding IsInteractionEnabled}"
                        Width="120" />
                    <Button
                        Command="{Binding LikeCommand}"
                        Content="❤️ Like"
                        Height="50"
                        IsEnabled="{Binding IsInteractionEnabled}"
                        Width="120" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

